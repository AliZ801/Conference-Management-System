@model CMS.Models.ViewModel.CMSViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";

    var title = "Add New Session Attendee";

    if (Model.SAttendee.Id != 0)
    {
        ViewData["Title"] = "Update Session Attendee";
    }
    else
    {
        ViewData["Title"] = "Add New Session Attendee";
    }
}

<div class="container">
    <div class="col-6 m-0 m-auto cus-border bg-light">
        <form method="post" asp-action="AddSAttendee" class="col-12">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @if (Model.SAttendee.Id != 0)
            {
                <input type="hidden" asp-for="@Model.SAttendee.Id" />
                title = "Update Session Attendee";
            }

            <div class="row justify-content-center">
                <h3 class="pt-2">@title</h3>
            </div>
            <hr class="w-75" />

            <div class="row form-group">
                <label>Session Title</label>
                @Html.DropDownListFor(i => i.SAttendee.SessionId, Model.SessionsList, "Select Session Title", new { @class = "form-control", @style="border-color:#0094ff" })
            </div>

            <div class="row form-group">
                <label>Attendee Name</label>
                @Html.DropDownListFor(i => i.SAttendee.AttendeeId, Model.AttendeesList, "Select Session Title", new { @class = "form-control", @style = "border-color:#0094ff" })
            </div>

            <div class="row form-group pb-2">

                @if (Model.SAttendee.Id != 0)
                {
                    <partial name="_EditAndBackBtn" model="@Model.SAttendee.Id" />
                }
                else
                {
                    <partial name="_CreateAndBackBtn" />
                }

            </div>

        </form>
    </div>
</div>

@section Scripts{
    <style>
        .cus-border {
            border: 2px solid #004481;
            border-top-left-radius: 50px;
            border-top-right-radius: 15px;
            border-bottom-left-radius: 15px;
            border-bottom-right-radius: 50px;
        }

        label {
            font-weight: 500;
        }

        hr {
            border-color: black;
        }
    </style>
}